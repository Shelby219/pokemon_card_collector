extends layout

block content

 div.title
  h1.title-text= title
   

  ul
  .pokemon-container
    each poke in pokes
      li.pokemon
       div.pokemon-div
        p.pokemon-p=poke.name
        p.pokemon-p=poke.type
        img(src=`${poke.image}`).pokemon-p
       
